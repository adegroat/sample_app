<%= provide(:title, 'All Users') %>

<h1 class="center">All Users</h1>

<ul class="users">
	<%= will_paginate(@users) %>

	<% @users.each do |user| %>
		<li>
			<%= gravatar_for(user) %>
			<%= link_to(user.name, user) %>
		</li>
	<% end %>

	<%= will_paginate(@users) %>
</ul>